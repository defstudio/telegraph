
parameters:
    level: max
    paths:
        - src
    excludePaths:
        - src/Support/Testing

    ignoreErrors:
        - '#expects string, mixed given#'
        - '#Parameter \#1 \$data of static method .+::fromArray\(\) expects array\{.+\}, mixed given#'
